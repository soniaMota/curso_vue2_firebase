// Si creo una instancia de Vue sin guardarla en una constante, como en los anteriores ejercicios
// desde la consola la puedo manejar usando la devtools de Vue.
// Ésta me crea una variable $vm0, que será la que asignará a Root
new Vue({
    el: 'main',
    data: {
        mensaje: 'Aprndiendo Vue.js :)'
    }
});